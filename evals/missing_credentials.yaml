name: hec_search_emails returns is_error when credentials missing
server:
  command: node
  args: [dist/index.js]
assert:
  tool: hec_search_emails
  args: {}
  expect:
    is_error: true
timeout: 30s
